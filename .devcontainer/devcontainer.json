{
  "name": "C Development Environment",
  "image": "mcr.microsoft.com/devcontainers/cpp:debian",

  "settings": {
    "github.copilot.enable": {
      "*": false
    },
    "editor.tabSize": 4,
    "editor.formatOnSave": true
  },

  "postCreateCommand": "apt-get update && apt-get install -y gcc-12 g++-12 make && update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-12 100 --slave /usr/bin/g++ g++ /usr/bin/g++-12 && apt-get clean && rm -rf /var/lib/apt/lists/*",

  "extensions": [
    "ms-vscode.cpptools"
  ]
}

